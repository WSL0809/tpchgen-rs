[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "tpchgen"
version = "0.1.0"
description = "Python bindings for tpchgen-rs (TPC-H data generator)"
requires-python = ">=3.8"

[tool.maturin]
bindings = "pyo3"
module-name = "tpchgen"
